services:
  api:
    build: ./apps/api
    ports:
      - "4000:4000"
    environment:
      - APP_VERSION=0.1.0

  web:
    build: ./apps/web
    ports:
      - "3000:3000"
    environment:
      - API_URL=http://api:4000
    depends_on:
      - api
